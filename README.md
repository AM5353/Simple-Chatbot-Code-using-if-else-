# Simple-Chatbot-Code-using-if-else-
📘 Short Description of the Project  This project is a rule-based chatbot created using Python’s basic input/output loop and if-elif-else statements. The chatbot takes user input and replies with predefined answers. If it does not understand the input, it shows a default response.
# Simple Rule-Based Chatbot (Python)

## 📌 Objective
Build a very basic chatbot using Python and simple if-else rules.

## 🛠️ Tools
- Python 3.x

## 📜 Description
This project demonstrates how a chatbot works using:
- Input/output loops  
- if-elif-else statements  
- Basic text matching  

The chatbot can respond to simple greetings, provide fixed answers, and exit when the user types "bye".

## ▶️ How to Run
1. Install Python  
2. Save the script as `chatbot.py`  
3. Run:
   ```bash
   python chatbot.py
